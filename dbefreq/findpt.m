function lz=findpt(xy)%function lz=findpt(xy)%% given a maxtrix xy containing longitude and latitude, find% all points in a box defined by user's input (ginput). Only% two inputs are allowed in defining the box. It is recommended% that the user select the lower left and the upper right points.%% used to select XBTs over land in the PXBT data set.% 8/13/97 jh%disp('Input two corners of box surrounding desired points');[xx,yy]=ginput(2);isheld=ishold;hold on;xx5=[min(xx) min(xx) max(xx) max(xx) min(xx)];yy5=[min(yy) max(yy) max(yy) min(yy) min(yy)];plot(xx5,yy5,'b');lz=find(xy(:,1)>=min(xx)&xy(:,1)<=max(xx)&xy(:,2)>=min(yy)&xy(:,2)<=max(yy));if length(lz)>0 plot(xy(lz,1),xy(lz,2),'bo'); else disp('No points found in box')endif isheld==0;hold off;end;