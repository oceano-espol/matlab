function geoquery(lon,lat,domain,id)% Function geoquery(LON,LAT,DOMAIN,ID)%   Plots geographic positions given by vectors LON,LAT on a map over %   DOMAIN =[minlon maxlon minlat maxlat]. If the matrix ID is also%   specified, each plotted point will be identified by its integer or %   string designator (the corresponding row of ID, e.g., the index of %   LON,LAT or a station name). Uses COASTMAP. %   Longitudes must be specified in degrees east of Greenwich. clg;global world;str=id;load world;coastmap(domain);hold onplot(lon,lat,'or')if nargin==4	n=length(id(:,1));	if isstr(id)==0;for k=1:n;str=int2str(id(k,:));end;end	for k=1:n		str=id(k,:);		if isstr(str)==0;str=int2str(id(k));end		text(lon(k)+1.5,lat(k),str)	endendhold off